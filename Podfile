# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'StayInTouch' do
  inhibit_all_warnings!
  use_frameworks!

  pod 'SwiftLint', '~> 0.30'
  pod 'SDWebImage', '~> 5.0'

end
